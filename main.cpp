/*
* Linear Algebra from assignment 3
* Implemented By Ha The Viet
* Date: 08/01/2021
*/

#include <iostream>
#include "constants.h"
#include "matrix.h"
#include "vector.h"

using namespace std;

int main(void)
{
    /*
     * For vectors
     */

    double *v, *w, *t;
    int size_v(5) , size_w(5) , size_t(5);

    // === allocation
    v = allocate(size_v);
    w = allocate(size_w);
    w = allocate(size_w);


    
    return 0;
}